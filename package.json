{
    "name": "@wealth-of-wisdom/monorepo",
    "version": "1.0.0",
    "description": "Smart contracts of Wealth of Wisdom",
    "private": true,
    "npmClient": "yarn",
    "license": "MIT",
    "scripts": {
        "test-all": "find ./packages -maxdepth 1 -type d -exec sh -c 'cd \"{}\" && [ -f foundry.toml ] && echo \"Testing in {}\" && forge test || echo \"No Foundry project in {}\"' \\;",
        "coverage-all": "find ./packages -maxdepth 1 -type d -exec sh -c 'cd \"{}\" && [ -f foundry.toml ] && echo \"Running coverage in {}\" && forge coverage || echo \"No Foundry project in {}\"' \\;",
        "prepare": "husky install && npm run create-hooks",
        "pre-commit": "npx lint-staged",
        "create-hooks": "npx husky set .husky/pre-commit \"npm run pre-commit\""
    },
    "lint-staged": {
        "packages/**/contracts/**/*.sol": [
            "yarn prettier --write",
            "yarn solhint -f table --max-warnings 0"
        ]
    },
    "repository": "git+https://github.com/wealth-of-wisdom/wow-contracts.git",
    "author": "Alina <alina.d@superhow.org>",
    "bugs": {
        "url": "https://github.com/wealth-of-wisdom/wow-contracts/issues"
    },
    "homepage": "https://github.com/wealth-of-wisdom/wow-contracts#readme",
    "dependencies": {},
    "devDependencies": {
        "husky": "^8.0.3",
        "lint-staged": "^15.2.0",
        "prettier": "^3.1.1",
        "prettier-plugin-solidity": "^1.3.1",
        "solhint": "^4.0.0",
        "solhint-plugin-prettier": "^0.1.0"
    },
    "workspaces": {
        "packages": [
            "packages/*"
        ]
    }
}