// stake WOW tokens -> reward: USD
// staking works in bands -> each has price list

// unstake
// can upgrade and downgrade

// can use several pools depending on level
// each pool has different return percentages
// pools are limited by level

// can upgrade to higher level and get more pool access
// downgrade level and get less pools, but return funds

//stake into band (set price) -> band stakes in all pools it unlocks (you get rewards from all pools in encompases)
