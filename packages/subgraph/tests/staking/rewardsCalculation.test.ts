import { describe, test, beforeEach, clearStore, assert } from "matchstick-as/assembly/index";

describe("calculateRewards() tests", () => {
    beforeEach(() => {
        clearStore();
    });

    describe("Create StakingContract entity", () => {});
});
